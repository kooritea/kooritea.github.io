{"id":2493,"content":"在二进制文件里面有的编码字符串会有字节顺序控制字符，以及结尾的结束字符，以及放在中间的分割字符，这些转换成字符串的时候不去掉的话就会变成零宽字符坑爹。\n\n字符串回写成二进制里面的时候字节顺序控制字符一般要加，但是结束的貌似加不加都可以，看别人的有的会加有的不会加。\n\n不加实际上也没有问题，因为一般二进制文件里面出现字符串的话都会在前面用几个字节记录字符串的长度","hide":"","time":"2020-08-19T17:52:41.000Z"}